type: object
description: Lightweight DTO for Result Codes, used in list/search views.
required: [id, code, title]
properties:
  id:
    type: integer
    format: int64
    description: Unique identifier.
  code:
    type: string
    description: Code that identifies the result.
  title:
    type: string
    description: Human-readable title.
