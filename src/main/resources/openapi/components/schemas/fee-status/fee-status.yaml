type: object
description: The status of the fee linked to the application. Blank fields will be rejected, nulls should be used instead.
required:
  - paymentStatus
  - statusDate

properties:

  paymentReference:
    type: string
    description: The reference code used to identify the payment.
    example: "PAY-001"
    maxLength: 15
    minLength: 1

  paymentStatus:
    $ref: './payment-status.yaml'

  statusDate:
    type: string
    format: date
    description: The date when the payment status was updated.
    example: "2025-12-01"
